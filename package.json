{
  "name": "@johntao/dummy-root",
  "private": true,
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build:examples": "pnpm --no-bail --workspace-concurrency 1 --filter '@example/*' build",
    "version": "pnpm i --no-frozen-lockfile"
  },
  "license": "MIT",
  "devDependencies": {
  },
  "packageManager": "pnpm@8.7.4",
  "pnpm": {
    "peerDependencyRules": {
      "ignoreMissing": [
        "@algolia/client-search",
        "playwright",
        "search-insights"
      ]
    }
  },
  "publishConfig": {
    "@johntao:registry": "https://npm.pkg.github.com"
  }
}
